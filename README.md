# Hướng dẫn chạy project TaskManagement (FrontEnd)

---

### 1. Cài đặt Base URL cho ứng dụng

Cập nhật đường dẫn Base Url của API tại enviroments/enviroment.ts

```bash
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5045'
};

```

---

### 2. Chạy project

Cmd/ terminal

```json
ng serve
```
